// Lógica de filtros y búsqueda sobre el array de pedidos.
var App = window.App || {};

App.applyFilters = function (orders, criteria) {
  // TODO: Etapa 2 — filtrar por estado PIM/VTEX/TMS, prioridad, búsqueda por orderId
  return orders;
};
