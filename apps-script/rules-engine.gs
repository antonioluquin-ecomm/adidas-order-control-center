// Motor de reglas: calcula la acción sugerida para cada pedido consolidado.
// Referencia: docs/reglas-estados.md

function applyRules(order) {
  // TODO: implementar lógica de reglas
  // Entrada: { orderId, pim: {status}, vtex: {status, delivered}, tms: {status} }
  // Salida:  { action, responsible, priority }
  return { action: null, responsible: null, priority: null };
}
