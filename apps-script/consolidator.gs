// Consolida pedidos de PIM, VTEX y TMS en un array unificado, haciendo join por orderId.

function consolidateOrders(pimRows, vtexRows, tmsRows) {
  // TODO: implementar join por orderId
  // Resultado esperado: array de objetos { orderId, pim, vtex, tms }
  return [];
}
